# Architecture

- `main.py` is the public local entrypoint.
- `backend/` contains the active API and service logic.
- `frontend/` contains the UI client.
- `docs/` contains supporting notes and historical guides.
- Changes should preserve the backend/frontend contract unless the task explicitly includes an API change.
