# 2025-12-15 개발 로그

## 변경/구현 상세
- 테스트/CI 최소 구성 추가
  - `requirements.txt`: `pytest`, `pytest-asyncio`를 테스트 용도로 포함.
  - `tests/`: FastAPI 앱을 ASGITransport로 붙인 `AsyncClient` 픽스처와 `/health` 확인용 기본 테스트 작성.
  - `.github/workflows/ci.yml`: push/PR 시 Python 3.11에서 의존성 설치 후 `pytest -q` 실행.
- README 보완
  - 루트 README에 로컬 테스트 실행 방법과 CI 트리거 정보를 추가해 개발 플로우를 명시.

## 검증/노트
- 로컬 `pytest -q` 통과(헬스 체크 단일 테스트).
- GitHub Actions 워크플로는 병합 후 리모트에서 자동 실행 예정.
