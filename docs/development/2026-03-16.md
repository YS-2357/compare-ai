# 2026-03-16

## Work
- Reorganized the repository around the approved local-first structure: `backend/`, `frontend/`, `plan/`, and a single public entrypoint in `main.py`.
- Removed the old `app/` tree and deleted unused runtime files and folders that no longer fit the current architecture.
- Simplified the backend to local chat comparison only and removed prompt-eval routes, schemas, and service modules from the active runtime.
- Simplified chat request handling by deleting turn-based controls and keeping conversation context through history only.
- Fixed backend stream aggregation so model-level failures are counted as real errors in the final summary instead of being treated as generic partial completions.
- Reworked the frontend session results view into line-by-line response output with explicit time, company, status, and response text.
- Improved startup ergonomics by detecting and reusing already-running Compare-AI processes on the expected local ports.
- Updated current docs and folder READMEs so the repo now documents the actual active architecture rather than the removed Streamlit/prompt-eval path.
