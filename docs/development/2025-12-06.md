# 2025-12-06 개발 로그

## 작업 내용
- 관리자 이메일 우회 및 모델 오버라이드 요구 사항 분석 → FastAPI/LangGraph/Streamlit 전반 업데이트
- Streamlit 사이드바에 모델 선택기 추가, `/api/ask` 요청에 `models` 필드 전달 구현
- 관리자 계정 멀티턴 제한 우회 로직 적용, Supabase/Upstash HTTP 타임아웃·종료 처리 정리
- README 및 모듈별 문서 업데이트 (Render 배포 구조, 문서 최신화 등)

## 테스트/검증
- 로컬 FastAPI + Streamlit 실행 후 관리자 계정으로 모델 선택·다중 턴 동작 확인
- Render 배포 명령(백엔드 `uvicorn`, 프론트 `streamlit`) 정리 및 문서화
