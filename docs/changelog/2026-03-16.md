# 2026-03-16

## Changes
- Renamed the active backend package from `app/` to `backend/` and updated the runtime entrypoint around `main.py`.
- Removed inactive hosted-runtime pieces from the active tree, including auth, rate limiting, scripts, tests, and other deployment-era leftovers.
- Removed the prompt-eval feature from the active runtime and kept the app focused on chat comparison only.
- Removed `turn`, `max_turns`, and related turn-limit handling from the active chat request and workflow path.
- Fixed chat summary accounting so backend summaries report real success and error counts per model.
- Updated the React frontend to show readable response rows instead of a raw event-inspector-first layout.
- Added startup port preflight checks so `main.py` reuses an existing Compare-AI backend/frontend process instead of half-starting on alternate ports.
- Refreshed current READMEs and planning documents to match the live repository structure.
